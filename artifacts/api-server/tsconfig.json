{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "noEmitOnError": false, // 👈 Add this temporarily
    "declaration": true,        // Generates the .d.ts files your routing needs
    "declarationMap": true,     // Maps the types back to the source code
    "outDir": "dist",
    "rootDir": "src",
    "types": ["node"]
  },
  "include": ["src"],
  "references": [
    {
      "path": "../../lib/db"
    },
    {
      "path": "../../lib/api-zod"
    }
  ]
}
